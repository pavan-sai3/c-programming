//write a program to print even numbers upto n using while loop

#include <stdio.h>
int n,i;
int main()
{   printf("Enter n value:");
    scanf("%d",&n);
    while(i<=n)
   { if(i%2==0)
       printf("%d\n",i);
     i++;
   }
  return 0;
}
